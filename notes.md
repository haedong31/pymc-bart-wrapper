# Notes on development of PyMC BART Wrapper

Generate a Python notebook version of `example_bart.py`, namely `example_bart.ipynb`,which contains two examples of using the `BARTModelWrapper` class for multiclass categorical and ordinal classification. The notebook should include explanations of the code and the purpose of each step, as well as visualizations of the results. Add markdown cells to explain the concepts and the code, for example, the data prerocessing of encoding categorical variables, data registration, the purpose of the `X_shared_` attribute in the `BARTModelWrapper` class, how it facilitates the generation of out-of-sample predictions using `pm.sample_posterior_predictive`, and what "Swap the shared covariate matrix with new data" means in the `predict` method by `self.X_shared_.set_value(X_new.values)`. Also, explain what "`pm.sample_posterior_predictive` to draw from the full generative model (BART trees + likelihood jointly)" means, especially what does BART + likelihood jointly mean. Refer to the docstrings of the `BARTModelWrapper` class and its methods for more details in `pymc_bart_wrapper.py`.

I have three questions. 1) What is the purpose of the `X_shared_` attribute in the `BARTModelWrapper` class, and how does it facilitate the generation of out-of-sample predictions using `pm.sample_posterior_predictive`? 2) What does "Swap the shared covariate matrix with new data" mean in the `predict` method by `self.X_shared_.set_value(X_new.values)`. 3) What does "`pm.sample_posterior_predictive` to draw from the full generative model (BART trees + likelihood jointly)." mean? Especially what does BART + likelihood jointly mean?

Why aren't `self.category_codes_`, `self.category_map_`, and `self.n_classes_` are used here although `encoder_fitted_` is `True`? Right now the code is learning the category codes of the target variable from the scracth.

Modyfiy `BARTModelWrapper` so that it allows users for providing entire dataset before splitting into training and testing sets and encoding categorical predictors and target variable. This way, the wrapper can handle the encoding of categorical variables internally, ensuring that the same encoding is applied to both training and testing sets, and that any unknown categories in the test set are handled appropriately. If any dataset is not provided before fitting the mode, the wrapper work as before, ecoding categorical variables in the training set and applying the same encoding to the test set, with unknown categories in the test set being encoded as all-zeros.

According to PyMC's official tutorial on Out-of-Sample Predictions in <https://www.pymc.io/projects/bart/en/latest/examples/bart_introduction.html#biking-with-bart>, `pm.sample_posterior_predictive` is used inside the `with model:` context to generate predictions from the posterior distribution of the model parameters. Why does the current wrapper class use a different approach? Is there a specific reason for this design choice, and does it have any implications for how the model is used or how predictions are generated?
